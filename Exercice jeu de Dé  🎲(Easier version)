import {  useState } from "react";

function Test() {
const[essais,setEssais]=useState(0);
const [message, setMessage] = useState("");
function startplayin(){
 const result= Math.floor(Math.random() * 6) + 1;//This line generates a random number between 1 and 6, which is perfect for a dice game 🎲.
  setEssais(prev => prev + 1);//Your program can run without this line ,It increases the number of tries by 1 every time the user plays.
 const usenambre= Number (document.getElementById("input").value);


 if (usenambre < 1 || usenambre >6){
   setMessage("⚠️ You must choose a number between 1 and 6");
 }
  else if (result === usenambre){
      setMessage  ("✅ Correct! Wanna play again?")
 }
 else{
 setMessage("❌ Wrong! The number was ")
 }
   
 
}
   return (
    <div>
      <h1> jeu de Dé🎲 </h1>
      <p> give us a nimbre betwin  1 and 6</p> <input type="text"  id="input"/><button onClick={startplayin}>play</button><p>{message}</p>
    </div>
  );
}

export default Test;
